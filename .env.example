# Docker Compose Environment Variables
# All variables are OPTIONAL for testing - the system will use sensible defaults

# JWT Secret (optional - has default for testing)
# For production, generate a secure random string
# JWT_SECRET=your-secret-key-change-this-in-production

# Cloudinary Settings (optional - will use local preview if not configured)
# Only needed if you want to upload images to Cloudinary
# VITE_CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
# VITE_CLOUDINARY_UPLOAD_PRESET=your-cloudinary-upload-preset

# Database (optional - defaults are set in docker-compose.yml)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=modelia
